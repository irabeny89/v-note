import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import NoteInputDialog from '../NoteInputDialog.vue'

// describe('HelloWorld', () => {
//   it('renders properly', () => {
//     const wrapper = mount(NoteInputDialog, { props: {  } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })
